# Terraform Readme

`terraform version`  
`brew install tfenv`  
`tfenv install latest`  
`tfenv use <version>`  

`cd terraform/deploy`  
`terraform init`  
`terraform fmt`  
`terraform validate`  
`terraform plan -out=tfplan`  
`terraform apply tfplan`
